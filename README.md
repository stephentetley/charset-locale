# charset-locale

Flix wrappers over `Charset` and `Locale` from the JDK.

License: Apache 2.0

News - version 0.11.0 - namespace changes modules are now 
in the namespace `InteropLocale`. Types have been pushed 
down one layer into their respective modules.
